name: deploy

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    uses: simplyenak/web-shared/.forgejo/workflows/deploy.yml@main
    with:
      site: cte
      expect: public
    secrets: inherit
