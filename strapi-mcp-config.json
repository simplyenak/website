{
  "mcpServers": {
    "strapi": {
      "command": "npx",
      "args": [
        "strapi-mcp@latest",
        "--url",
        "http://localhost:1337"
      ],
      "env": {
        "STRAPI_API_TOKEN": "${STRAPI_API_TOKEN}"
      }
    }
  }
}
