---
import Icon from '@/components/Icon.astro'
let { icon, title, href, buttonType } = Astro.props
---

<a href={href} class={buttonType}>
  <span>{title}</span>
  {icon && <Icon name={icon} class="size-5" />}
</a>
